project.ext.deployDir = project.hasProperty('portalLibDir') ? project.portalLibDir : null

evaluationDependsOn(':polls-api')

dependencies {
	compile project(':polls-api')

	compile files(["${project.ext.liferayProjectDir}/portal-impl/classes"])
}
